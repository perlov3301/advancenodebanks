Continuation Passing Style means asyncronious function in functional programming:
 control is passed explicity in the form of a continuation (this is contrasted with 
 direct style)= a function takes an extra argument: an explicit "continuation",i.e.
  a function of one argument with asyncronious futures